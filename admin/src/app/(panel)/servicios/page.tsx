import { RidesList } from '@/features/rides/rides-list';

export const metadata = {
  title: 'Servicios - Panel administrativo',
};

export default function ServiciosPage() {
  return <RidesList />;
}
