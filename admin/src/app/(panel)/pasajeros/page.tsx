import { PassengersList } from '@/features/passengers/passengers-list';

export const metadata = {
  title: 'Pasajeros - Panel administrativo',
};

export default function PasajerosPage() {
  return <PassengersList />;
}
