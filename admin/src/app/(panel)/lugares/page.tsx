import { PlacesList } from '@/features/places/places-list';

export const metadata = {
  title: 'Lugares y tarifas - Panel administrativo',
};

export default function LugaresPage() {
  return <PlacesList />;
}
